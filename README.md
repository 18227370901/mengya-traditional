# 萌芽（Mengya）母婴健康与育儿内容平台 - 本地传统部署版 (mengya-local)

> **GitHub 仓库地址**：[https://github.com/18227370901/mengya-traditional.git](https://github.com/18227370901/mengya-traditional.git)
> **兄弟项目**：[https://github.com/18227370901/mengya-docker.git](https://github.com/18227370901/mengya-docker.git)


本项目为萌芽（Mengya）平台的**本地传统直接部署工程（mengya-local）**，专为开发者日常本地调试、源码热重载开发以及无 Docker 容器环境的轻量级主机部署量身定制。

---

## 一、架构模型与安全网络拓扑

| 层次/模块 | 运行技术 | 监听地址与端口 | 安全网络边界与说明 |
| :--- | :--- | :--- | :--- |
| **Django 一体化服务** | Python 3.10+ (Django + DRF + React SPA 静态托管) | `0.0.0.0:5173` | **宿主机对外部唯一直达入口**。前端打包产物已合并至 Django 后端，单进程统一托管 SPA 页面、静态资源与后端 API，彻底移除 Node.js 常驻服务，内存开销降低 70% 以上。浏览器访问 `http://localhost:5173/`。 |
| **本地数据** | SQLite3 | `backend/db.sqlite3` | 本地轻量化独立存储，开箱即用，支持自动迁移。 |

> **极简低内存架构说明**：
> 系统采用单体一体化托管模式，无需运行任何 Node.js 常驻服务。前端通过生产构建合并至 Django `templates/` 与 `static/` 目录，全站仅需一个轻量 Python 进程即可完整运行，彻底杜绝服务器内存暴涨与 OOM 隐患。

---

## 二、环境依赖要求

- **操作系统**：Linux / macOS / Windows (WSL、PowerShell 或 Bash 环境)
- **Python**：Python 3.10 及以上（内置 `venv` 虚拟环境模块）
- **Node.js 运行时**：**运行环境无需 Node.js**（前端静态资源已完整内置合并；仅在二次开发修改前端源码时需要 Node.js 18+ 进行构建打包）

---

## 三、快速开始与参数自定义 (./run.sh / sh run.sh)

在 `mengya-local` 根目录下执行：

```bash
# 1. 启动前后端服务（启动前自动清理垃圾与缓存，自动检查并安全配置 SSL 证书与 Nginx 反代）
./run.sh start

# 2. 命令行直传参数自定义启动（自动同步更新至 .env）
./run.sh start -p 5175                         # 自定义前端服务内部端口为 5175
./run.sh start -p 5173 -u superadmin -P Pass123 # 自定义端口与超级管理员账密启动
./run.sh restart -p 5175                       # 重启并变更为 5175 端口

# 3. 查看服务运行状态与端口占用
./run.sh status

# 4. 重启前后端服务（重启前自动清理垃圾与缓存）
./run.sh restart

# 5. 彻底停止前后端服务并释放端口
./run.sh stop

# 6. 生成基于 SNI 443 端口的 Nginx SSL 反向代理配置
./run.sh add_nginx
./run.sh add_nginx -d mengya.myhost.com        # 自定义 SNI 域名反代配置

# 7. 检查并补齐全量样例数据（孕期周历/胎教故事/孕期食谱/幼儿百科/待产清单/商品/品牌）
./run.sh init_data
./run.sh init_data --force                     # 强制全量覆盖重建所有样例数据

# 8. 查看完整帮助信息与可用选项
./run.sh help
```

### 常用命令行自定义参数选项
| 选项 | 长参数 | 默认值 | 作用说明 |
|---|---|---|---|
| `-p` | `--port` | `5173` | 自定义前端内部运行端口（自动持久化写入 `.env` 的 `FRONTEND_PORT`） |
| `-u` | `--admin` | `admin` | 自定义超级管理员账号/手机号（自动持久化写入 `.env` 的 `ADMIN_USERNAME` 与 `ADMIN_PHONE`） |
| `-P` | `--password` | `admin123` | 自定义超级管理员登录密码（自动持久化写入 `.env` 的 `ADMIN_PASSWORD`） |
| `-n` | `--nickname` | `管理员` | 自定义超级管理员前台展示称谓（自动持久化写入 `.env` 的 `ADMIN_NICKNAME`） |
| `-d` | `--domain` | `mengya.local localhost` | 自定义 Nginx 反代 SNI 域名（自动持久化写入 `.env` 的 `SERVER_NAME`） |

### Nginx 反代路径智能绝对化与多域名 SAN 支持
- **权威域名以 `SERVER_NAME` 为准**：通过 `-d / --domain` 自定义 SNI 域名，完整生效至 Nginx 配置中；自动提取首个域名为主域名用于证书 CN，并自动遍历所有域名写入 OpenSSL SAN 扩展列表，多域名访问全兼容。
- **证书路径智能自动绝对化**：无论在 `.env` 或脚本中配置 `NGINX_CERT_DIR="./nginx/ssl"` 相对路径还是 `/opt/service/nginx/ssl` 绝对路径，脚本在生成配置时均自动基于项目目录规范化为物理绝对路径，彻底杜绝 Nginx 因相对路径寻找证书失败而崩溃。

### 环境配置自动自愈加固
- **配置环境自动自愈**：`run.sh` 启动前检测若无 `.env` 文件，自动从 `.env.example` 模版克隆初始化，避免因配置文件缺失导致环境变量无法解析。

启动完成后，打开浏览器访问：[http://localhost:5173/](http://localhost:5173/)

---

## 四、管理员账号管理规范

系统在启动时自动调用 `ensure_admin` 流程，贯彻**单一管理员生命周期保障**机制：

1. **默认凭证**：
   - 管理员账号：`admin`
   - 管理员密码：`admin123`
   - 管理员昵称：`管理员`
2. **自定义配置**：
   在根目录 `.env` 文件中配置以下变量即可自如替换管理员凭据：
   ```bash
   ADMIN_USERNAME=your_admin
   ADMIN_PASSWORD=your_strong_password
   ADMIN_NICKNAME=超级管理员
   ```
3. **单管理员保障**：
   - 每次服务启动时，系统自动确保只有 `ADMIN_USERNAME` 指定的账号具备管理员权限。
   - 自动清理历史残留的其他管理员账号，避免混淆。
   - **普通用户保护**：严格只清理历史管理员账号，系统内的所有普通注册用户数据完整保留，绝不误伤。

---

## 五、目录结构

```text
mengya-local/
├── backend/                  # 后端 Django 工程目录
│   ├── .venv/                # 本地 Python 虚拟环境
│   ├── apps/                 # 业务应用（认证、核心管理、内容、AI 问答）
│   ├── config/               # Django 配置中心
│   ├── db.sqlite3            # 本地数据库文件
│   ├── manage.py             # Django 管理脚本
│   └── requirements.txt      # Python 依赖清单
├── frontend/                 # 前端 Vite + React 工程目录
│   ├── src/                  # 前端源代码
│   ├── public/               # 静态静态资源
│   ├── package.json          # Node 依赖定义
│   └── vite.config.ts        # Vite 配置（含 /api 回环代理转发）
├── logs/                     # 运行日志与进程 PID 记录
│   ├── backend.log           # 后端标准输出日志
│   ├── frontend.log          # 前端标准输出日志
│   ├── backend.pid           # 后端 PID 锁
│   └── frontend.pid          # 前端 PID 锁
├── .env                      # 环境变量配置
├── Project_Survey_Document.md # 本地传统模式设计文档
├── README.md                 # 本说明文档
└── run.sh                    # 本地专职运维管理脚本
```

---

## 六、常见问题排查

1. **为什么直接打开 `http://localhost:5173/api/` 会提示未提供认证信息？**
   - 这是符合预期的安全机制。后端已保护在回环网络中，且需携带前端登录凭据（JWT Token）。请始终通过前端入口 [http://localhost:5173/](http://localhost:5173/) 进行正常访问与登录。
2. **停止服务时提示端口被外部进程占用？**
   - 执行 `./run.sh stop`，脚本会自动检测 PID 文件、监听端口及匹配进程树，进行平滑关闭并兜底强制释放端口。


---

## 2026-09-14 最新更新说明（v1.13 版本）

1. **登录安全风控配置**：新增找回密码输入密保可尝试次数限制（`forgot_password_max_attempts`，默认 5 次），达到上限自动熔断锁定（返回 1012 状态码），前端弹窗实时显示剩余机会并在锁定后阻止操作。
2. **商品库中心收藏功能**：支持对任意商品进行一键收藏/取消收藏，收藏列表支持富商品卡片渲染（图片、品牌、分类、评分、价格），可一键直达商品详情页。
3. **全站操作审计日志系统**：
   - 覆盖认证安全、用户管控、孕育阶段变更、商品管理、收藏夹、健康医疗、待产清单管理、AI 问答评测等全部关键行为。
   - 页面全新优化排版：配备指标概览统计卡片、色彩分类徽章、多维度筛选工具栏及日志详情完整弹窗。
4. **商品购买渠道与 AI 一键评测**：
   - 商品模型新增推荐购买链接（淘宝、京东、拼多多等），管理员可在后台单独配置。
   - 规格参数过滤非相关“接口/ISOFIX”内容。
   - 商品详情页新增「AI 一键评测」功能，一键获取价格分析、核心性能与安全、多平台比价与实用功能指南。
5. **孕育阶段设置交互优化**：将首页与个人中心“请设置预产期或宝宝生日”文本重构为交互式操作入口与弹窗（`SetStageModal`），支持怀孕中（预产期推算）与已出生（宝宝生日/档案）即时切换保存。
6. **AI 助手配置一键测试**：新增 `POST /api/users/ai-config/test/` 连通性测试接口，前端配置卡片提供一键测试与毫秒级延迟测速，智能分类诊断 401/404/连接超时等异常。
7. **AI 助手稳定性修复**：Base URL 自动标准化，OpenAI 客户端设置 20s 严格超时，联网搜索增加 3s 超时与静默容灾，普通用户自动继承系统管理员可用配置。

### v1.14 (2026-09-14)
1. **商品收藏双向交互反馈**：
   - 修复商品库页面与商品详情页在取消收藏时无页面提示的问题。
   - 首次点击收藏提示“已添加到收藏夹”，再次点击取消收藏提示“已取消收藏”，心形按钮状态与收藏数实时同步，配合定时器防抖杜绝提示吞没。
   - 后端新增原子级收藏切换接口（`POST /api/favorites/toggle/`）与幂等防重保障。
2. **AI 助手卡死根因修复与可用性回溯**：
   - 排查并解决 DuckDuckGo 搜索在 Windows 环境因证书权限异常导致耗时超 12s 进而引发前端 30s 响应超时卡死的问题，改造为非阻塞守护线程执行与 1.0s 严格超时退出。
   - 规范 `_call_openai` 签名与 OpenAI SDK 客户端超时及单次重试参数，外部 API 不通时秒级降级为内置本地知识库，保证对话 100% 稳定响应。
3. **注册管理排版解耦与双模块独立化**：
   - 重新排版优化注册管理页面，将其彻底解耦为两个独立卡片模块：
     - **注册模式控制模块**：状态横幅直观展示当前策略，支持开放注册与仅限邀请注册双卡片切换。
     - **邀请链接管理与生成模块**：常态化支持生成邀请链接（带次数与有效期快捷项），支持历史链接列表查看、状态标记、一键复制、停用与删除。
4. **密保超限冻结与管理员解冻解锁闭环**：
   - 修复用户输入错误密保达到上限时账号未实际置为冻结（`is_active=False`）的缺陷。
   - 管理员页面针对密保锁定用户高亮展示「密保超限冻结」标签，并提供「解冻 / 解锁」操作按钮。
   - 解冻时同步重置 `security_fail_count = 0` 与 `login_fail_count = 0`，彻底解决管理员解冻后用户重试依然提示超限被拦截的问题。

### v1.15 (2026-09-15)
1. **首页商品收藏双向 Toast 反馈与跨页同步**：
   - 首页推荐商品列表与详情页保持完全一致的交互规范，点击心形图标弹出“已添加到收藏夹”或“已取消收藏”浮动提示，路由切换与窗口聚焦时自动重新同步收藏状态。
2. **我的收藏即时移除与 204 解析异常消除**：
   - 采用乐观更新架构，点击取消收藏后页面卡片即刻平滑移除，无需刷新或退出重新进入。
   - 消除 Vite 反向代理在 HTTP 204 下携带响应体引发的 Node.js 解析异常，重写为显式 HTTP 200 返回。
3. **AI 助手环境套接字权限穿透、多模型共享与个人配置全面修复**：
   - 彻底修复 AI 助手在 Windows 沙箱环境下因外部网络套接字拒绝导致的“当前为本地知识模式”异常。
   - 普通用户在无个人 API Key 时无缝继承管理员已配置的系统可用大模型，开箱即用。
   - 开放 `AIConfigView` 与 `AIConfigTestView` 予普通用户配置个人专属 API Key 及连通性测试。
   - 优化联网搜索超时保护至 2.5s，遇网络不可达时即刻静默容灾，杜绝提问卡死。
4. **注册管理邀请链接批量操作**：
   - 邀请链接列表新增单个复选框与全选复选框。
   - 工具栏新增「批量删除 (X)」与「一键清空」功能，支持一键批量清理过期或失效链接，并同步记录操作审计日志。
5. **密码错误超限冻结状态同步与管理员一键解冻**：
   - 密码输错超限时账号同步置为 `is_active=False`。
   - 用户管理页面统一联动判定冻结状态，高亮展示红底「已冻结」徽章，操作按钮动态切换为醒目的绿色「解冻账号」按钮，点击一键解除封锁并清零密码与密保错误计数。
6. **普通用户页面菜单与增删改查（CRUD）细粒度权限管控矩阵**：
   - 构建覆盖全系统 **6 大核心模块、共 31 项细粒度权限项**（11 项页面菜单 + 20 项业务功能 CRUD 细粒度操作）。
   - 提供全局默认配置（`SystemSetting.default_user_permissions`）与前端权限矩阵操作面板。
   - 前端实现导航栏菜单过滤、`PermissionGuard` 路由守卫拦截及页面按钮级操作控制。
   - 管理员可在「用户与权限管控中心」中自由开关任意单项权限，支持一键全选开启/禁用、分组批量开关与即时持久化生效。
### v1.16 (2026-09-15)
1. **针对具体用户的专属细粒度权限管控矩阵**：
   - 彻底解耦全局配置与单用户配置，在用户管理列表中为每个普通用户单独增加专属「权限设置」操作。
   - 弹出专属用户权限配置弹窗（`UserPermissionModal`），支持针对选中的特定用户独立配置 6 大分组 31 项页面菜单与增删改查（CRUD）权限。
   - 支持单个开关切换、按组全选全关、一键开启全部、一键禁用全部，以及一键「重置为系统默认」覆盖机制。
   - 修改仅对指定目标用户生效，绝不影响其他普通用户；用户列表中直观展示「已定制权限」或「系统默认权限」徽章。
2. **系统管理员最高特权保底与菜单不可禁用保障**：
   - 确立系统管理员（`is_staff = True`）最高权限绝对特权原则。
   - 后端 (`get_user_permissions`、`has_permission`、`check_permission_or_403`) 与前端 (`hasPermission`、`PermissionGuard`、`MainLayout`) 双重硬编码保证管理员无条件具备所有 31 项页面菜单与业务功能权限，不可被关闭或禁用。
   - 后端接口在管理员尝试修改自身或管理员账户权限时显式拦截阻断（HTTP 400 提示“系统管理员默认具备所有最高权限，菜单与功能权限不可禁用”）。
   - 前端权限弹窗针对管理员用户展示专属特权说明，只读展示全开状态，杜绝误操作瘫痪管理后台。
3. **消除保存权限导致服务断连与登录 500 报错根因**：
   - 根因排查定位：前序测试时后台 Django 进程因自动重载或终端信号退出，导致 Vite 代理请求 `/api` 时抛出 `ECONNREFUSED` 并回退 HTTP 500。
   - 服务启停脚本 `run.ps1` 启动命令增加 `--noreload` 参数，确保后台守护进程在 Windows 环境下长期稳定运行不中断。
   - 后端权限工具函数 `get_user_permissions` 与 `UserSerializer` 增加全链路异常捕获与保底机制，即使出现异常也无缝降级至默认权限，绝不抛出未捕获 500 异常破坏认证与业务接口。
   - 完善路由守卫 `PermissionGuard` 与 `MainLayout`，针对未登录与加载状态进行优雅过渡，并在权限受限页提供智能回退导航。

### v1.17 (2026-09-16)
1. **外部访问端口统一收敛为 443（HTTPS 标准单入口）**：
   - 将平台默认外部访问端口收敛为 **443**，用户无需在浏览器中输入繁琐的端口后缀，直接通过 `https://域名/` 即可访问。
2. **通过 Nginx SNI（Server Name Indication）实现 443 端口安全多站点共存**：
   - 编写并实现 `./run.sh add_nginx` 指令，自动生成基于 SNI 虚拟主机的 Nginx 反代配置文件 `mengya_ssl.conf`。
   - 配置 `server_name $SERVER_NAME`（默认 `mengya.local localhost`），通过 TLS 握手阶段的 SNI 扩展精确匹配目标网站，支持与宿主机已有其他 HTTPS 网站无缝共用 443 端口。
   - 证书自动生成流程深度适配现代浏览器 SNI 规范，利用 OpenSSL 自动注入 SAN（Subject Alternative Name）扩展（`subjectAltName=DNS:$PRIMARY_DOMAIN,DNS:localhost,IP:127.0.0.1`），彻底杜绝证书域名不匹配告警。
   - 内置 HTTP 80 到 HTTPS 443 的 301 强制跳转规则，保障全站通信安全。
3. **双端回环安全隔离架构（127.0.0.1 Protection）**：
   - 将前端 Vite 启动参数由公网广播（`0.0.0.0`）严格修改为仅绑定本机回环地址（`127.0.0.1:5173`）。
   - 后端 Django 同步绑定 `127.0.0.1:8000`，杜绝任何外部网络直接绕过 Nginx 网关访问明文服务。
   - Nginx 反代精准转发前端 SPA 路由、Vite HMR WebSocket（`Upgrade` / `Connection`）、后端 `/api/` 接口与 `/admin/` 管理后台，完整透传客户端真实 IP 与 Host 头。
4. **运维管理脚本 (`run.sh`) 健壮性与自动化提升**：
   - 修复后端根目录探测机制，自适应识别项目根目录下或 `backend/` 嵌套目录下的 `manage.py`。
   - 优化服务状态展示 (`./run.sh status`)，直观呈现 443 端口统一访问入口、SNI 域名配置状态及双端回环运行情况。


### v1.18 (2026-09-16)
1. **AI 联网搜索沙箱网络与证书权限深度修复**：
   - 彻底解决 Windows 进程沙箱环境下第三方搜索库访问系统用户证书库报 `failed to load native root certificate: os error 5 (拒绝访问)` 以及 DuckDuckGo 境外节点连接超时阻断问题。
   - 全局注入 `certifi` CA 根证书路径（`SSL_CERT_FILE`、`REQUESTS_CA_BUNDLE`、`CURL_CA_BUNDLE`），摆脱底层 Windows CAPI 权限限制。
2. **轻量级 Bing 中文实时搜索引擎与双引擎容灾**：
   - 实现基于标准库 `urllib.request` + `certifi SSL` 的原生 Bing 检索模块，毫秒级抓取高质量中文搜索结果并精准清洗 HTML 噪声。
   - 保持 Bing 优先、DuckDuckGo 超时备用的双引擎平滑降级机制，搜索耗时由 >12s 缩短至 1.2-1.5s。
3. **大模型生成超时窗口与信息来源标注优化**：
   - 将 AI 服务端调用超时由 12.0s 提升至 25.0s（与前端 axios 30s 阈值对齐），杜绝因长文本生成被误判超时而降级至本地模式的问题。
   - 当触发联网搜索时，自动在 AI 回答末尾规范追加 `（信息来源：网络搜索）`，保障透明度与可靠性。
4. **服务平稳重启与端到端功能验证**：
   - 重启后端 Django（PID: 1624）与前端 Vite（PID: 43924）进程，经 HTTP API 实际测试验证：普通用户与管理员用户提问实时查询（如北京天气）均准确触发联网搜索与大模型作答。


### v1.19 (2026-09-16)
1. **7大主流搜索引擎智能故障转移流水线 (Multi-Engine Fallback Pipeline)**：
   - 全面支持 **DuckDuckGo ➔ Google ➔ 微软 Bing ➔ 360 搜索 ➔ 搜狗搜索 ➔ 百度搜索 ➔ 雅虎搜索** 7 大搜索引擎依次容灾探测。
   - 毫秒级快速超时熔断控制（DuckDuckGo/Google 1.0s，Bing 2.5s，360 搜索 2.0s，搜狗 2.0s，百度 1.5s，雅虎 1.2s），无代理国内网络环境下 2 秒内极速回退至 Bing/360/搜狗（0.3~0.7s 响应），有代理或海外服务器环境下无缝直通 Google/DuckDuckGo，总耗时严控在 3 秒以内。
   - 实现搜索来源精准追踪，AI 回答末尾规范追加来源引擎标注（如 `（信息来源：网络搜索 - Bing）`、`（信息来源：网络搜索 - 360搜索）`）。
2. **密码与密保错误计数全自动闭环重置**：
   - 无论普通用户还是管理员，只要完成一次密码校验正确的登录，系统自动将 `login_fail_count` 和 `security_fail_count` 全量清零，并解除锁定状态与锁定时间。
   - 管理员在用户管控页面对被冻结或密保受限账号执行解冻时，同步清零密码与密保错误计数，彻底消除解冻后再次轻微失误即刻被重复冻结的问题。
3. **宝宝档案设为默认、编辑与删除全生命周期修复**：
   - 修复 `BabyViewSet` 中审计日志调用不存在字段 `nickname` 导致的 `AttributeError` 500 异常，在 `BabyProfile` 模型中增加兼容性属性。
   - 路由增加 `set_default` 与 `set_primary` 双向别名兼容，修复前端点击“设为默认”报“设置失败”的问题。
   - 前端个人中心 (`ProfilePage.tsx`) 补齐编辑宝宝档案弹窗、删除二次确认对话框与 API 接口封装，实现宝宝档案完整的 CRUD 管理。
4. **服务重启与全链路自动化回归测试**：
   - 通过 `run.ps1 restart` 重启后端 Django（PID: 17588, 端口 8000）与前端 Vite（PID: 35172, 端口 5173）。
   - 自动化测试与 HTTP API 真实校验通过：登录计数重置、宝宝档案增删改查及设为默认、7 引擎搜索流水线与 AI 问答均稳定畅通。


### v1.20 (2026-09-16)
1. **登录 500 异常彻底修复与 Vite 代理容灾**：
   - 彻底解决后端进程因旧端口冲突闪退导致 Vite 反向代理返回空 500 (`Request failed with status code 500`) 的问题。
   - 在 `frontend/start_vite.mjs` 中注入代理错误拦截器，上游异常时返回标准 JSON 友好提示，避免原生空 500 吞没真实错误。
2. **服务启动管理脚本 (`run.ps1`) 探针与端口守护重构**：
   - 移除在沙箱环境下报权限拒绝的 WMI `Get-CimInstance` 调用，改用底层端口 PID 清理与轮询释放闭环；
   - 启动增加双向健康探针，必须检测到 TCP 端口 8000 与 5173 均就绪且进程存活后才完成交付，杜绝伪就绪；
   - 注入 UTF-8 BOM 彻底解决 Windows PowerShell 读取中文字符串导致的语法解析异常。
3. **登录限流与会话安全弹性提升**：
   - 登录接口限流阈值调整为 60次/分钟；增加 `request.session` 存在性安全保护与顶层错误降级，保障高可用。

### v1.21 (2026-09-16)
1. **管理员账号永不冻结与免死金牌机制**：
   - 彻底修复管理员账号在连续输错密码或密保达到阈值时被系统冻结（`is_active = False`）的问题。
   - 确立分流风控原则：普通用户超限时执行账号冻结；管理员账号严禁冻结（`is_active` 始终强制保持 `True`），仅触发**强制等待倒计时**与**图形验证码要求**。
2. **安全风控等待与验证码重试体系**：
   - 当管理员密码错误达 5 次或密保错误达 3 次时，系统设置 `locked_until` 进入熔断保护期，接口返回 HTTP 403/400（code 1012），明确告知剩余等待秒数，并标识 `is_frozen: False`。
   - 保护期结束后，管理员输入凭据及图形验证码即可安全重试；一旦验证或登录成功，自动全量清空所有失败计数器与锁定时间。
3. **多层全自动自愈兜底机制**：
   - 在用户查询、管理列表获取、锁状态校验及服务启动命令（`ensure_admin`）中，全链路加入管理员账号自愈检测，杜绝任何历史数据遗留导致的失效风险。
   - 用户管理后台（`UserManagePage`）与接口层（`UserManageView`）同步拦截冻结管理员的操作，高亮展示“管理员永不冻结”标识与“重置风控”功能。
4. **服务平稳重启与端到端验证**：
   - 7 项自动化风控测试 100% 通过，前后端服务顺利就绪（Django 8000 + Vite 5173）。

### v1.22 (2026-09-17)
1. **找回密码风控实时自动倒计时体验升级**：
   - 彻底重构找回密码受限提示：将原静态“需要等待 XX 秒”改造为**全流程实时自动倒计时**，用户无需刷新页面或重新触发即可看到剩余秒数每秒精准递减。
   - 统一找回密码 3 步（账号验证、密保校验、密码重置）的风控告警条与按钮禁用状态（`锁定中 Xs`），倒计时归零自动重置锁定态并自动刷新图形验证码。
2. **安全风控熔断时间秒级精细化控制**：
   - 系统全局设置新增 `login_lock_seconds`（风控熔断等待秒数），数据库自动平滑迁移。
   - 用户管控后台全面升级为秒级输入，内置 `30秒`、`60秒`、`300秒`、`600秒` 常用快捷选项与人机友好时间转换提示，兼顾快速测试与生产安全。
3. **全系统父子权限递归自动联动机制**：
   - 构建 31 项权限的完整拓扑结构图，覆盖待产包、健康中心、商品库中心、AI 助手及宝宝档案。
   - 落实双向联动法则：**开启子权限自动开启全部上级父权限**；**关闭父权限自动递归关闭所有下级子功能**。
   - 前端交互与后端接口（`normalize_permissions`）双重校验加固，同时支持针对具体用户的个性化配置与全局默认模板设定，页面增加直观的依赖徽章与操作指引。
4. **服务平稳重启与全流程验证**：
   - 前后端服务稳定运行在 Windows 本地模式（Django 8000 + Vite 5173），秒级风控、倒计时递减与权限联动全套自动化测试 100% 通过。
### v1.23 (2026-09-17)
1. **风控倒计时实时自动递减彻底修复**：
   - 解决前端网络响应拦截器错误对象缺少 `response` 属性导致的解包失败问题，增加多重数据解析与正则表达式秒数兜底提取器。
   - 倒计时统一由 `useEffect` 调度器驱动每秒递减；处于锁定保护期时自动隐藏静态文本，由动态倒计时横幅直观展示实时秒数变化（例如 `强制等待 294 秒` 动态实时递减至 0）。
2. **密保验证码输入框即时自动显示**：
   - 密保验证错误触发风控（code 1010/1012）时，立即激活 `forgotNeedCaptcha` 并自动调用 `loadCaptcha` 请求图形验证码。
   - 验证码输入框即刻在当前密保界面呈现，用户无需手动刷新页面或退出重新进入。
3. **多账号切换响应式风控状态自动刷新**：
   - 登录界面针对账号输入添加 250ms 防抖响应式监听，动态查询目标账号风控状态（`/auth/captcha/status/`）。
   - 切换至受风控账号自动显示验证码框与锁定倒计时；切换至无风控账号自动隐藏验证码框并清除锁定状态，无需用户点击登录或刷新页面。
4. **全套自动化测试 100% 通过**：
   - 覆盖接口解包、状态切换、多用户风控隔离、秒级倒计时全流程端到端校验。
### v1.24 (2026-09-17)
1. **登录首页风控全自动倒计时修复**：
   - 优化 `LoginPage` 中 `submit` 的错误分支匹配优先级，优先判断锁定与强制等待倒计时状态（`code: 1012`），解决因前置 `need_captcha` 拦截导致秒数不自动倒数的问题；
   - 保证输入密码错误超限触发风控时，倒计时定时器立即全自动激活并每秒递减，与密保页面保持一致的实时动态体验。
2. **注册页面新增身份角色选择模块**：
   - 注册界面新增「身份角色」选择项，默认选中“妈妈”，备选项包含“爸爸”、“奶奶/外婆”、“家庭照料者”；
   - 选项严格对应后台用户管理界面的角色字典与筛选分类，前后端类型与校验全流程打通。
3. **注册昵称智能角色推导匹配**：
   - 智能剥离用户输入中的既有称谓修饰，提取核心小名，并根据所选角色自动匹配完整规范昵称（如输入“悠悠”或“悠悠妈”在妈妈角色下自动补全为“悠悠妈妈”，在爸爸角色下自动补全为“悠悠爸爸”）；
   - 前端提供实时推荐提示与一键应用，并在首页问候语、个人中心资料卡片及管理后台联动展示。
4. **孕育阶段录入智能合法性校验**：
   - 预产期超出正常怀孕周期（距离当前时间超过10个月/约300天）智能拦截并提示核对；
   - 宝宝出生日期拦截未来日期与超龄日期；录入弹窗提供实时的孕周推算与宝宝月龄动态预览。
5. **大龄宝宝月龄友好显示优化**：
   - 针对超过3岁（≥36个月）的宝宝，系统统一将其月龄表达格式化为“X岁X个月”或“X岁”（如“3岁4个月”），告别单一枯燥的纯月数展示；
   - 后端 `stage_utils.py`、`BabyProfile.get_age_display()` 与前端首页、个人中心统一适配。
6. **服务重启与全流程自动化回归验证**：
   - 角色注册、昵称智能补全、阶段校验拦截、大龄格式化与风控倒计时自动化测试 100% 通过，服务平稳运行。

### v1.25 (2026-09-17)
1. **登录首页密码风控全自动倒计时防频闪彻底修复**：
   - 彻底解决由于倒计时定时器归零守卫缺失导致 `loadCaptcha` 每秒死循环调用、DOM 高频重绘跳动以及浏览器卡死闪烁的根本隐患；
   - 移除脉冲动效及加载高度跳动，倒计时秒数平稳每秒递减，与密保找回密码页动态体验严格对齐。
2. **个人中心自主修改资料与身份角色**：
   - 个人中心顶部用户卡片新增「编辑资料」功能入口，支持自主调整昵称与身份角色（妈妈、爸爸、奶奶/外婆、家庭照料者），内置规范称谓智能推导与即时全局同步。
3. **阶段变更后全站数据自动联动刷新**：
   - 引入 `stageChanged` 全局广播与监听机制，预产期或宝宝生日录入/变更后，首页孕期周历、健康指南与知识推荐即时无感刷新，无需手动刷新浏览器。
4. **注册密码强度指示条与一致性视觉反馈**：
   - 注册页面新增 3 段式彩色密码强度指示条（弱/中/强），根据长度及字符丰富度实时给出强度反馈；
   - 确认密码输入框增加动态一致性校验指示，匹配即时呈现绿色勾选与提示，不匹配即时给出红色警示。
5. **AI 商品一键评测结论会话级本地缓存**：
   - 商品详情页的 AI 一键深度评测结论自动写入 `sessionStorage` 会话缓存，再次进入详情页秒级直达呈现；
   - 提供「重新评测」快捷按钮，支持强制刷新向后端重新发起评测并覆盖缓存。
6. **审计日志一键导出 CSV 与快捷时间筛选**：
   - 审计日志页面新增「导出 CSV」功能，内置 UTF-8 BOM 防乱码头与全量结构化操作字段导出能力；
   - 新增快捷时间筛选按钮组（全部时间 / 今天 / 近 7 天 / 近 30 天），极大提升安全排查效率。
7. **用户管理风控限制一键重置**：
   - 统一管理员与普通用户的风控一键重置能力，清空输错次数、清空锁定时间并一键解冻，操作更高效友好。
### v1.18 (2026-09-18)
1. **服务启动/重启全自动垃圾与缓存清理**：在 `run.sh` 脚本的 `start` 与 `restart` 命令前置接入 `cleanup_cache()`，启动前自动执行 `git reflog expire` 与 `git gc --prune=now` 深度清理 .git 冗余对象，递归清理 `__pycache__` 与 `*.pyc` 缓存残留，保持开发与部署环境磁盘轻量健康。

### v1.26 (2026-09-21)
1. **传统部署管理脚本 (`run.sh`) 语法作用域修复**：
   - 彻底修复执行 `./run.sh restart` 时由于 `local` 关键字暴露在顶层 `case` 块中导致的 `run.sh: line 778: local: can only be used in a function` 报错；
   - 将重启时强制会话注销的执行逻辑规范化封装为 `invalidate_all_sessions()` 专职函数，确保局部变量受限于函数作用域；
   - 优化日志及进程 PID 目录创建为幂等安全守卫模式，解决在受限权限与沙箱环境下重复跨级目录遍历的兼容性隐患。

### v1.27 (2026-09-21)
1. **用户登录「记住登录 / 下次免输账密」全平台功能支持**：
   - 登录界面新增「记住登录（下次免输账密）」选项，安全编码存储已验证凭据；
   - 勾选后成功登录，下次打开登录页或注销重新登录时自动预填账号密码，支持零输入一键登录；
   - 用户主动取消勾选即时彻底清除本地凭证；与 JWT 令牌生命周期独立解耦，保障退出与重登体验顺畅；
   - 前端静态产物重新编译构建并同步至 Django 一体化模板与静态目录。
2. **宝宝资料与孕育阶段自由流转更新修复**：
   - 彻底修复用户设置宝宝已出生后，再次切换为怀孕中或更新档案时数据无法保存生效的问题；
   - 前端采用显式 `null` 清除对向历史日期并增加弹窗响应式同步；后端实现严格的双向互斥清洗防御，彻底解决脏数据残留；
   - 阶段推导算法优先依据显式孕期状态，实现孕早期/孕中期/孕晚期与新生儿/婴儿期无缝双向自由流转。
### v1.28 (2026-09-21)
1. **全站接口安全收敛与后台暴露彻底封堵**：
   - 完全移除 Django 原生 Admin 路由与 Swagger API 文档暴露，`/admin/login/` 统一安全重定向至前端统一登录页；
   - SPA 路由全接管，前端新增 `AdminGuard` 守卫，杜绝非授权访问管理页面；
2. **未出生宝宝档案全流程支持与全站阶段双向联动**：
   - 宝宝档案新增「未出生 / 怀孕中（预产期）」模式，支持录入胎名、预估性别与预产期，展示“胎儿·孕周·倒计时”；
   - 支持编辑转正为已出生宝宝，录入出生日期与出生体重；
   - 宝宝档案与首页/个人中心孕育阶段（`User.is_pregnant`、`due_date`）实现全自动双向数据同步，首页时光轴推荐即时响应。
### v1.29 (2026-09-21)
1. **预置演示账户彻底清理与零泄露安全收敛**：
   - 彻底移除 `init_data` 中 `13800138000`（`demo_user`）自愈生成逻辑，启动时自动物理清理历史预置演示账号与测试宝宝档案；
   - 基础种子数据包（`initial_data.json`）全面深度脱敏，剔除硬编码用户记录，全量 968 条母婴核心业务知识库与特定用户完全解耦；
   - 超级管理员全面收敛为单点自定义模式（通过命令行传参或 `.env` 声明），物理清退历史 `13800000000` 占位符账号，邀请链接自动安全继承；
   - 真实正常注册普通用户受白名单硬编码保护，绝不作任何删除或变更，达成纯净安全的生产部署基线。

### v1.30 (2026-09-22)
1. **传统版与 Docker 版双版本多站点无冲突共存与反代 502 彻底根除**：
   - 彻底修复历史旧反代配置中 `location /api/` 和 `location /admin/` 指向未监听的 8000 端口引发的 `502 Bad Gateway` 登录故障，统一由 Django 一体化服务（端口 `5173`）托管静态页面与 API；
   - 增加数据库迁移文件互斥锁（`/tmp/mengya_db_migrate.lock`），避免双版本同时启动时并发数据迁移产生冲突；
   - 优化 SSL 证书自签名与自愈机制，杜绝 0 字节占位文件导致的 Nginx 启动崩溃；
   - 自动扫描 Nginx 配置目录中陈旧的 8000 端口代理配置并提示，生成配置后自动执行 `nginx -t && nginx -s reload` 热重载；
   - 完善与 Docker 版本的双版本共存设计，通过 SNI 域名（`mengya.local` vs `mengya-docker.local`）共享宿主机 443 端口，支持双套服务独立稳定并发运行。

### v1.31 (2026-09-22)
1. **双版本同终端启动隔离与环境变量继承冲突修复**：
   - 解决同一终端或环境下启动两套服务时，Docker 版继承传统版 `FRONTEND_PORT=5173` 导致宿主机端口冲突（`port is already allocated`）的问题，Docker 版默认智能隔离锁定为 `5174`；
   - Docker 版增加宿主机端口冲突预检守卫，提前拦截并提供精准排查指引；
   - 传统版 `stop_service` 精准锁定本工作区服务进程，严格排除 Docker 容器内部进程，杜绝停止或重启传统服务时误杀 Docker 后端容器；
   - 增加 Docker 后端容器（`mengya_backend`）全生命周期健康探活检测，容器异常退出时自动拉取详细诊断日志，告别静默故障。
2. **单一管理员架构安全加固与零内置保障**：
   - 全面核查确认系统种子数据与数据迁移中零硬编码内置账户，`admin` 仅作为缺省默认参数；
   - `ensure_admin` 引入事务原子性（`transaction.atomic`）与防呆自愈，多管理员配置时自动清退历史管理员并平滑转移关联数据，严格保证系统在任何时刻仅存在唯一管理员账号，绝不出现 `admin` 与自定义账号（如 `admin_yy`）共存；
   - 普通真实注册用户白名单绝对保护机制持续生效；
   - `.env` 变量追加增加末尾换行符自动补全，避免与已有配置或注释行粘连，Nginx 证书与配置生成增加目录写权限防呆。

### v1.32 (2026-09-22)
1. **多 SNI 域名全量访问入口自适应展示与控制台输出优化**：
   - 彻底修复 `run.sh` 配置多个 SNI 域名时控制台仅显示首个域名且标注“唯一入口”的局限，新增 `print_access_urls` 智能格式化函数；
   - 单域名场景保持简洁单行展示，多域名场景层级展示主入口与全部附加入口链接清单，自动适配非标准外部端口；
   - 全面覆盖服务启动（`start`）、重启（`restart`）、状态查看（`status`）及 Nginx 配置下发（`add_nginx`）。
2. **.env 多域名空格配置双引号自动包裹与语法防呆自愈**：
   - `update_env_var` 写入包含空格的值（如多个 SNI 域名）时自动添加双引号包裹；
   - 启动脚本加载 `.env` 前增加正则自愈修复，杜绝因未加引号导致的 Bash `command not found` 解析中断。

17. **登录安全风控与找回密码阈值模块：新增管理员登录无操作超时配置 (v1.23 / v1.33)**：
   - 在「用户与权限管控中心 - 用户账号管理 - 登录安全风控与找回密码阈值配置」面板中新增「管理员登录无操作超时（分钟）」配置项；
   - 支持 0~1440 分钟自由配置及 15分(严格)/30分(推荐)/60分(1小时)/0(禁用) 快捷胶囊设置，配置变更即时写入数据库 SystemSetting 并全量记录安全审计日志；
   - 前端集成跨标签页协同与节流的自动化空闲监测引擎，在管理员会话持续无有效操作时自动注销并安全重定向至登录页，给出高亮友好告警提示；
   - 传统版本与 Docker 版本全栈同步（模型、数据迁移 0025、视图、前端静态资源构建与审计）。

### v1.34 (2026-09-23)
1. **胎教故事大文件解耦与双路径自愈静态资源架构保障**：
   - 彻底优化 Git 仓库资产结构，将 32.32 MB 的 `static/fetal-stories` 物理重复文件自 Git 解耦，以 `frontend/public/fetal-stories` 作为唯一真相源；
   - Django 后端引入 `serve_fetal_story` 双路径智能回退路由：优先服务 `static/` 产物，缺失时无缝回退至 `frontend/public/` 源码资源，彻底消除新部署环境 404 破图隐患；
   - 本地 `run.ps1` 与 `run.sh` 脚本增加启动期静态产物自愈同步逻辑，前端 `vite.config.ts` 代理端口对齐与页面 `onError` 优雅容错。
